{
  "name": "@auto-demo/capture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src/index.test.ts src/capturePaths.test.ts src/captureEvents.test.ts src/jsonlEventWriter.test.ts src/playwrightMetadataRecorder.test.ts src/playwrightAdapter.test.ts src/captureManifest.test.ts",
    "test:smoke": "vitest run src/*.smoke.test.ts",
    "setup:browser": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
