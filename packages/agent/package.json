{
  "name": "@auto-demo/agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "npm run build -w @auto-demo/project && npm run build -w @auto-demo/polish",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "npm run build -w @auto-demo/project && npm run build -w @auto-demo/polish",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build -w @auto-demo/project && npm run build -w @auto-demo/polish",
    "test": "vitest run src/index.test.ts"
  },
  "dependencies": {
    "@auto-demo/polish": "0.0.0",
    "@auto-demo/project": "0.0.0"
  }
}
