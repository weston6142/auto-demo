{
  "name": "@auto-demo/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "autodemo": "dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "npm run build -w @auto-demo/polish",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "npm run build -w @auto-demo/polish",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build -w @auto-demo/polish",
    "test": "vitest run src/index.test.ts src/defaultBackend.test.ts"
  },
  "dependencies": {
    "@auto-demo/capture": "0.0.0",
    "@auto-demo/polish": "0.0.0",
    "@auto-demo/project": "0.0.0"
  }
}
