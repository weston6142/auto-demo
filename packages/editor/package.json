{
  "name": "@auto-demo/editor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "npm run build -w @auto-demo/project",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "npm run build -w @auto-demo/project",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build -w @auto-demo/project",
    "test": "vitest run src/index.test.ts",
    "test:unit": "vitest run src/index.test.ts"
  },
  "dependencies": {
    "@auto-demo/project": "0.0.0"
  }
}
